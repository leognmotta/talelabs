{
  "name": "@talelabs/flows",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "check-types": "tsc --noEmit && tsc --project tsconfig.scripts.json",
    "dev:build": "tsc --watch --preserveWatchOutput",
    "generation:check": "tsx scripts/check-generation-registry.ts",
    "provider-output:check": "tsx scripts/check-generation-provider-outputs.ts",
    "run:check": "tsx scripts/check-run-planner.ts",
    "lint": "eslint ."
  },
  "dependencies": {
    "@noble/hashes": "^2.2.0",
    "@talelabs/models-catalog": "*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "tsx": "^4.7.1",
    "typescript": "5.9.2"
  }
}
