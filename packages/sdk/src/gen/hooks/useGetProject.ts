/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { Client, RequestConfig, ResponseErrorConfig } from "../../client";
import type {
  GetProjectQueryResponse,
  GetProjectPathParams,
  GetProject401,
  GetProject403,
  GetProject404,
} from "../types/GetProject.ts";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { getProject } from "../clients/getProject.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getProjectQueryKey = ({
  projectId,
}: {
  projectId: GetProjectPathParams["projectId"] | undefined;
}) =>
  [{ url: "/projects/:projectId", params: { projectId: projectId } }] as const;

export type GetProjectQueryKey = ReturnType<typeof getProjectQueryKey>;

export function getProjectQueryOptions(
  { projectId }: { projectId: GetProjectPathParams["projectId"] | undefined },
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getProjectQueryKey({ projectId });
  return queryOptions<
    GetProjectQueryResponse,
    ResponseErrorConfig<GetProject401 | GetProject403 | GetProject404>,
    GetProjectQueryResponse,
    typeof queryKey
  >({
    enabled: !!projectId,
    queryKey,
    queryFn: async ({ signal }) => {
      return getProject(
        { projectId: projectId! },
        { ...config, signal: config.signal ?? signal },
      );
    },
  });
}

/**
 * {@link /projects/:projectId}
 */
export function useGetProject<
  TData = GetProjectQueryResponse,
  TQueryData = GetProjectQueryResponse,
  TQueryKey extends QueryKey = GetProjectQueryKey,
>(
  { projectId }: { projectId: GetProjectPathParams["projectId"] | undefined },
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetProjectQueryResponse,
        ResponseErrorConfig<GetProject401 | GetProject403 | GetProject404>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? getProjectQueryKey({ projectId });

  const query = useQuery(
    {
      ...getProjectQueryOptions({ projectId }, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<
    TData,
    ResponseErrorConfig<GetProject401 | GetProject403 | GetProject404>
  > & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
