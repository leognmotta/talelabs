/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { Client, RequestConfig, ResponseErrorConfig } from "../../client";
import type {
  GetProductQueryResponse,
  GetProductPathParams,
  GetProduct401,
  GetProduct403,
  GetProduct404,
} from "../types/GetProduct.ts";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { getProduct } from "../clients/getProduct.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getProductQueryKey = ({
  productId,
}: {
  productId: GetProductPathParams["productId"] | undefined;
}) =>
  [{ url: "/products/:productId", params: { productId: productId } }] as const;

export type GetProductQueryKey = ReturnType<typeof getProductQueryKey>;

export function getProductQueryOptions(
  { productId }: { productId: GetProductPathParams["productId"] | undefined },
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getProductQueryKey({ productId });
  return queryOptions<
    GetProductQueryResponse,
    ResponseErrorConfig<GetProduct401 | GetProduct403 | GetProduct404>,
    GetProductQueryResponse,
    typeof queryKey
  >({
    enabled: !!productId,
    queryKey,
    queryFn: async ({ signal }) => {
      return getProduct(
        { productId: productId! },
        { ...config, signal: config.signal ?? signal },
      );
    },
  });
}

/**
 * {@link /products/:productId}
 */
export function useGetProduct<
  TData = GetProductQueryResponse,
  TQueryData = GetProductQueryResponse,
  TQueryKey extends QueryKey = GetProductQueryKey,
>(
  { productId }: { productId: GetProductPathParams["productId"] | undefined },
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetProductQueryResponse,
        ResponseErrorConfig<GetProduct401 | GetProduct403 | GetProduct404>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? getProductQueryKey({ productId });

  const query = useQuery(
    {
      ...getProductQueryOptions({ productId }, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<
    TData,
    ResponseErrorConfig<GetProduct401 | GetProduct403 | GetProduct404>
  > & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
