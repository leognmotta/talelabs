/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { Client, RequestConfig, ResponseErrorConfig } from "../../client";
import type {
  GetBrandQueryResponse,
  GetBrandPathParams,
  GetBrand401,
  GetBrand403,
  GetBrand404,
} from "../types/GetBrand.ts";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { getBrand } from "../clients/getBrand.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getBrandQueryKey = ({
  brandId,
}: {
  brandId: GetBrandPathParams["brandId"] | undefined;
}) => [{ url: "/brands/:brandId", params: { brandId: brandId } }] as const;

export type GetBrandQueryKey = ReturnType<typeof getBrandQueryKey>;

export function getBrandQueryOptions(
  { brandId }: { brandId: GetBrandPathParams["brandId"] | undefined },
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getBrandQueryKey({ brandId });
  return queryOptions<
    GetBrandQueryResponse,
    ResponseErrorConfig<GetBrand401 | GetBrand403 | GetBrand404>,
    GetBrandQueryResponse,
    typeof queryKey
  >({
    enabled: !!brandId,
    queryKey,
    queryFn: async ({ signal }) => {
      return getBrand(
        { brandId: brandId! },
        { ...config, signal: config.signal ?? signal },
      );
    },
  });
}

/**
 * {@link /brands/:brandId}
 */
export function useGetBrand<
  TData = GetBrandQueryResponse,
  TQueryData = GetBrandQueryResponse,
  TQueryKey extends QueryKey = GetBrandQueryKey,
>(
  { brandId }: { brandId: GetBrandPathParams["brandId"] | undefined },
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetBrandQueryResponse,
        ResponseErrorConfig<GetBrand401 | GetBrand403 | GetBrand404>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getBrandQueryKey({ brandId });

  const query = useQuery(
    {
      ...getBrandQueryOptions({ brandId }, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<
    TData,
    ResponseErrorConfig<GetBrand401 | GetBrand403 | GetBrand404>
  > & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
