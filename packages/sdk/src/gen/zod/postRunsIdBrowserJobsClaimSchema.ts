/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import * as z from "zod";
import type {
  PostRunsIdBrowserJobsClaim200,
  PostRunsIdBrowserJobsClaim400,
  PostRunsIdBrowserJobsClaim401,
  PostRunsIdBrowserJobsClaim403,
  PostRunsIdBrowserJobsClaim404,
  PostRunsIdBrowserJobsClaim409,
  PostRunsIdBrowserJobsClaim429,
  PostRunsIdBrowserJobsClaim500,
  PostRunsIdBrowserJobsClaimMutationRequest,
  PostRunsIdBrowserJobsClaimMutationResponse,
  PostRunsIdBrowserJobsClaimPathParams,
} from "../types/PostRunsIdBrowserJobsClaim.ts";
import { cuid2Schema } from "./cuid2Schema.ts";
import { errorResponseSchema } from "./errorResponseSchema.ts";

export const postRunsIdBrowserJobsClaimPathParamsSchema = z.object({
  get id() {
    return cuid2Schema;
  },
}) as unknown as z.ZodType<PostRunsIdBrowserJobsClaimPathParams>;

/**
 * @description Dependency-ready browser jobs
 */
export const postRunsIdBrowserJobsClaim200Schema = z.object({
  jobs: z.array(
    z.object({
      debugOutputs: z.nullable(
        z.array(
          z.union([
            z.object({
              delivery: z.enum(["text"]),
              mimeType: z.enum(["text/plain"]),
              outputIndex: z.int().min(0),
              text: z.string(),
            }),
            z.object({
              delivery: z.enum(["url"]),
              metadata: z
                .object({})
                .catchall(z.union([z.boolean(), z.string(), z.number()])),
              mimeType: z.string(),
              outputIndex: z.int().min(0),
              url: z.url(),
            }),
          ]),
        ),
      ),
      executionContract: z.object({
        modelId: z.string(),
        nodeId: z.string(),
        operationId: z.string(),
        providerBinding: z.union([
          z.union([
            z.object({
              lifecycle: z.union([
                z.object({
                  cancellation: z.enum([
                    "best-effort",
                    "supported",
                    "unsupported",
                  ]),
                  completions: z
                    .array(z.enum(["response"]))
                    .min(1)
                    .max(1),
                  deliveries: z
                    .array(z.enum(["bytes", "stream", "text", "url"]))
                    .min(1),
                  submission: z.enum(["immediate"]),
                }),
                z.object({
                  cancellation: z.enum([
                    "best-effort",
                    "supported",
                    "unsupported",
                  ]),
                  completions: z.union([
                    z
                      .array(z.enum(["poll"]))
                      .min(1)
                      .max(1),
                    z
                      .array(z.enum(["webhook"]))
                      .min(1)
                      .max(1),
                    z
                      .array(z.union([z.enum(["poll"]), z.enum(["webhook"])]))
                      .min(2)
                      .max(2),
                    z
                      .array(z.union([z.enum(["webhook"]), z.enum(["poll"])]))
                      .min(2)
                      .max(2),
                  ]),
                  deliveries: z
                    .array(z.enum(["bytes", "stream", "text", "url"]))
                    .min(1),
                  submission: z.enum(["asynchronous"]),
                }),
              ]),
              nativeModelId: z.string().regex(/^[^/]+\/.+$/),
              operationId: z.string().min(1),
              provider: z.enum(["openrouter"]),
              providerTag: z.string().min(1),
              endpoint: z.enum(["/api/v1/chat/completions"]),
              protocol: z.enum(["audio"]),
              requestProfile: z.object({
                kind: z.enum(["audio"]),
                outputFormat: z.enum(["wav"]),
                settingIds: z.array(z.string().min(1)),
              }),
            }),
            z.object({
              lifecycle: z.union([
                z.object({
                  cancellation: z.enum([
                    "best-effort",
                    "supported",
                    "unsupported",
                  ]),
                  completions: z
                    .array(z.enum(["response"]))
                    .min(1)
                    .max(1),
                  deliveries: z
                    .array(z.enum(["bytes", "stream", "text", "url"]))
                    .min(1),
                  submission: z.enum(["immediate"]),
                }),
                z.object({
                  cancellation: z.enum([
                    "best-effort",
                    "supported",
                    "unsupported",
                  ]),
                  completions: z.union([
                    z
                      .array(z.enum(["poll"]))
                      .min(1)
                      .max(1),
                    z
                      .array(z.enum(["webhook"]))
                      .min(1)
                      .max(1),
                    z
                      .array(z.union([z.enum(["poll"]), z.enum(["webhook"])]))
                      .min(2)
                      .max(2),
                    z
                      .array(z.union([z.enum(["webhook"]), z.enum(["poll"])]))
                      .min(2)
                      .max(2),
                  ]),
                  deliveries: z
                    .array(z.enum(["bytes", "stream", "text", "url"]))
                    .min(1),
                  submission: z.enum(["asynchronous"]),
                }),
              ]),
              nativeModelId: z.string().regex(/^[^/]+\/.+$/),
              operationId: z.string().min(1),
              provider: z.enum(["openrouter"]),
              providerTag: z.string().min(1),
              endpoint: z.enum(["/api/v1/chat/completions"]),
              protocol: z.enum(["chat"]),
              requestProfile: z.object({
                kind: z.enum(["chat"]),
                maxImageReferences: z.int().min(0),
                maxTokensParameter: z.enum([
                  "max_completion_tokens",
                  "max_tokens",
                ]),
                reasoning: z.boolean(),
                settingIds: z.array(z.string().min(1)),
              }),
            }),
            z.object({
              lifecycle: z.union([
                z.object({
                  cancellation: z.enum([
                    "best-effort",
                    "supported",
                    "unsupported",
                  ]),
                  completions: z
                    .array(z.enum(["response"]))
                    .min(1)
                    .max(1),
                  deliveries: z
                    .array(z.enum(["bytes", "stream", "text", "url"]))
                    .min(1),
                  submission: z.enum(["immediate"]),
                }),
                z.object({
                  cancellation: z.enum([
                    "best-effort",
                    "supported",
                    "unsupported",
                  ]),
                  completions: z.union([
                    z
                      .array(z.enum(["poll"]))
                      .min(1)
                      .max(1),
                    z
                      .array(z.enum(["webhook"]))
                      .min(1)
                      .max(1),
                    z
                      .array(z.union([z.enum(["poll"]), z.enum(["webhook"])]))
                      .min(2)
                      .max(2),
                    z
                      .array(z.union([z.enum(["webhook"]), z.enum(["poll"])]))
                      .min(2)
                      .max(2),
                  ]),
                  deliveries: z
                    .array(z.enum(["bytes", "stream", "text", "url"]))
                    .min(1),
                  submission: z.enum(["asynchronous"]),
                }),
              ]),
              nativeModelId: z.string().regex(/^[^/]+\/.+$/),
              operationId: z.string().min(1),
              provider: z.enum(["openrouter"]),
              providerTag: z.string().min(1),
              endpoint: z.enum(["/api/v1/images"]),
              protocol: z.enum(["image"]),
              requestProfile: z.object({
                kind: z.enum(["image"]),
                maxReferences: z.int().min(0),
                settingIds: z.array(z.string().min(1)),
              }),
            }),
            z.object({
              lifecycle: z.union([
                z.object({
                  cancellation: z.enum([
                    "best-effort",
                    "supported",
                    "unsupported",
                  ]),
                  completions: z
                    .array(z.enum(["response"]))
                    .min(1)
                    .max(1),
                  deliveries: z
                    .array(z.enum(["bytes", "stream", "text", "url"]))
                    .min(1),
                  submission: z.enum(["immediate"]),
                }),
                z.object({
                  cancellation: z.enum([
                    "best-effort",
                    "supported",
                    "unsupported",
                  ]),
                  completions: z.union([
                    z
                      .array(z.enum(["poll"]))
                      .min(1)
                      .max(1),
                    z
                      .array(z.enum(["webhook"]))
                      .min(1)
                      .max(1),
                    z
                      .array(z.union([z.enum(["poll"]), z.enum(["webhook"])]))
                      .min(2)
                      .max(2),
                    z
                      .array(z.union([z.enum(["webhook"]), z.enum(["poll"])]))
                      .min(2)
                      .max(2),
                  ]),
                  deliveries: z
                    .array(z.enum(["bytes", "stream", "text", "url"]))
                    .min(1),
                  submission: z.enum(["asynchronous"]),
                }),
              ]),
              nativeModelId: z.string().regex(/^[^/]+\/.+$/),
              operationId: z.string().min(1),
              provider: z.enum(["openrouter"]),
              providerTag: z.string().min(1),
              endpoint: z.enum(["/api/v1/audio/speech"]),
              protocol: z.enum(["speech"]),
              requestProfile: z.object({
                kind: z.enum(["speech"]),
                outputFormats: z
                  .array(z.enum(["mp3"]))
                  .min(1)
                  .max(1),
                settingIds: z.array(z.string().min(1)),
                voiceValues: z.object({}).catchall(z.string().min(1)),
              }),
            }),
            z.object({
              lifecycle: z.union([
                z.object({
                  cancellation: z.enum([
                    "best-effort",
                    "supported",
                    "unsupported",
                  ]),
                  completions: z
                    .array(z.enum(["response"]))
                    .min(1)
                    .max(1),
                  deliveries: z
                    .array(z.enum(["bytes", "stream", "text", "url"]))
                    .min(1),
                  submission: z.enum(["immediate"]),
                }),
                z.object({
                  cancellation: z.enum([
                    "best-effort",
                    "supported",
                    "unsupported",
                  ]),
                  completions: z.union([
                    z
                      .array(z.enum(["poll"]))
                      .min(1)
                      .max(1),
                    z
                      .array(z.enum(["webhook"]))
                      .min(1)
                      .max(1),
                    z
                      .array(z.union([z.enum(["poll"]), z.enum(["webhook"])]))
                      .min(2)
                      .max(2),
                    z
                      .array(z.union([z.enum(["webhook"]), z.enum(["poll"])]))
                      .min(2)
                      .max(2),
                  ]),
                  deliveries: z
                    .array(z.enum(["bytes", "stream", "text", "url"]))
                    .min(1),
                  submission: z.enum(["asynchronous"]),
                }),
              ]),
              nativeModelId: z.string().regex(/^[^/]+\/.+$/),
              operationId: z.string().min(1),
              provider: z.enum(["openrouter"]),
              providerTag: z.string().min(1),
              endpoint: z.enum(["/api/v1/videos"]),
              protocol: z.enum(["video"]),
              requestProfile: z.object({
                frameMode: z.enum(["first", "first-last", "none"]),
                generateAudio: z.boolean(),
                kind: z.enum(["video"]),
                referenceLimits: z.object({
                  audio: z.int().min(0),
                  image: z.int().min(0),
                  video: z.int().min(0),
                }),
                referenceValidationPolicy: z.enum([
                  "none",
                  "seedance-2-reference-v1",
                ]),
                settingIds: z.array(z.string().min(1)),
                totalReferenceLimit: z.optional(z.int().gt(0)),
              }),
            }),
          ]),
          z.object({
            endpoint: z.enum(["https://queue.fal.run"]),
            lifecycle: z.union([
              z.object({
                cancellation: z.enum([
                  "best-effort",
                  "supported",
                  "unsupported",
                ]),
                completions: z
                  .array(z.enum(["response"]))
                  .min(1)
                  .max(1),
                deliveries: z
                  .array(z.enum(["bytes", "stream", "text", "url"]))
                  .min(1),
                submission: z.enum(["immediate"]),
              }),
              z.object({
                cancellation: z.enum([
                  "best-effort",
                  "supported",
                  "unsupported",
                ]),
                completions: z.union([
                  z
                    .array(z.enum(["poll"]))
                    .min(1)
                    .max(1),
                  z
                    .array(z.enum(["webhook"]))
                    .min(1)
                    .max(1),
                  z
                    .array(z.union([z.enum(["poll"]), z.enum(["webhook"])]))
                    .min(2)
                    .max(2),
                  z
                    .array(z.union([z.enum(["webhook"]), z.enum(["poll"])]))
                    .min(2)
                    .max(2),
                ]),
                deliveries: z
                  .array(z.enum(["bytes", "stream", "text", "url"]))
                  .min(1),
                submission: z.enum(["asynchronous"]),
              }),
            ]),
            nativeModelId: z.string().regex(/^[^/]+\/.+$/),
            operationId: z.string().min(1),
            protocol: z.enum(["queue"]),
            provider: z.enum(["fal"]),
            providerTag: z.enum(["fal-queue"]),
            requestProfile: z.union([
              z.object({
                combinedParams: z.array(
                  z.object({
                    field: z.string().min(1),
                    settingIds: z.array(z.string().min(1)).min(2).max(2),
                    valueMap: z.object({}).catchall(
                      z.object({}).catchall(
                        z.union([
                          z.boolean(),
                          z.string(),
                          z.number(),
                          z.object({
                            height: z.int().gt(0),
                            width: z.int().gt(0),
                          }),
                        ]),
                      ),
                    ),
                  }),
                ),
                inputMappings: z.array(
                  z.object({
                    alternativeFields: z.optional(
                      z.array(
                        z.object({
                          field: z.string().min(1),
                          mediaType: z.enum(["audio", "image", "video"]),
                        }),
                      ),
                    ),
                    cardinality: z.enum(["many", "single"]),
                    field: z.string().min(1),
                    maxItems: z.int().gt(0),
                    mediaType: z.enum(["audio", "image", "video"]),
                    minItems: z.int().min(0),
                    targetSlotId: z.string().min(1),
                  }),
                ),
                kind: z.enum(["image"]),
                maxInputItems: z.int().min(0),
                params: z.array(
                  z.object({
                    field: z.string().min(1),
                    numberMultiplier: z.optional(z.number().gt(0)),
                    sendWhen: z.optional(
                      z.object({
                        equals: z.union([z.boolean(), z.string(), z.number()]),
                        settingId: z.string().min(1),
                      }),
                    ),
                    settingId: z.string().min(1),
                    valueMap: z.optional(
                      z
                        .object({})
                        .catchall(
                          z.union([z.boolean(), z.string(), z.number()]),
                        ),
                    ),
                  }),
                ),
                promptField: z.nullable(z.string().min(1)),
                requestedCountField: z.nullable(z.string().min(1)),
                settingIds: z.array(z.string().min(1)),
                staticParams: z
                  .object({})
                  .catchall(z.union([z.boolean(), z.string(), z.number()])),
              }),
              z.object({
                combinedParams: z.array(
                  z.object({
                    field: z.string().min(1),
                    settingIds: z.array(z.string().min(1)).min(2).max(2),
                    valueMap: z.object({}).catchall(
                      z.object({}).catchall(
                        z.union([
                          z.boolean(),
                          z.string(),
                          z.number(),
                          z.object({
                            height: z.int().gt(0),
                            width: z.int().gt(0),
                          }),
                        ]),
                      ),
                    ),
                  }),
                ),
                inputMappings: z.array(
                  z.object({
                    alternativeFields: z.optional(
                      z.array(
                        z.object({
                          field: z.string().min(1),
                          mediaType: z.enum(["audio", "image", "video"]),
                        }),
                      ),
                    ),
                    cardinality: z.enum(["many", "single"]),
                    field: z.string().min(1),
                    maxItems: z.int().gt(0),
                    mediaType: z.enum(["audio", "image", "video"]),
                    minItems: z.int().min(0),
                    targetSlotId: z.string().min(1),
                  }),
                ),
                kind: z.enum(["speech"]),
                maxInputItems: z.int().min(0),
                params: z.array(
                  z.object({
                    field: z.string().min(1),
                    numberMultiplier: z.optional(z.number().gt(0)),
                    sendWhen: z.optional(
                      z.object({
                        equals: z.union([z.boolean(), z.string(), z.number()]),
                        settingId: z.string().min(1),
                      }),
                    ),
                    settingId: z.string().min(1),
                    valueMap: z.optional(
                      z
                        .object({})
                        .catchall(
                          z.union([z.boolean(), z.string(), z.number()]),
                        ),
                    ),
                  }),
                ),
                promptField: z.nullable(z.string().min(1)),
                settingIds: z.array(z.string().min(1)),
                staticParams: z
                  .object({})
                  .catchall(z.union([z.boolean(), z.string(), z.number()])),
              }),
              z.object({
                combinedParams: z.array(
                  z.object({
                    field: z.string().min(1),
                    settingIds: z.array(z.string().min(1)).min(2).max(2),
                    valueMap: z.object({}).catchall(
                      z.object({}).catchall(
                        z.union([
                          z.boolean(),
                          z.string(),
                          z.number(),
                          z.object({
                            height: z.int().gt(0),
                            width: z.int().gt(0),
                          }),
                        ]),
                      ),
                    ),
                  }),
                ),
                inputMappings: z.array(
                  z.object({
                    alternativeFields: z.optional(
                      z.array(
                        z.object({
                          field: z.string().min(1),
                          mediaType: z.enum(["audio", "image", "video"]),
                        }),
                      ),
                    ),
                    cardinality: z.enum(["many", "single"]),
                    field: z.string().min(1),
                    maxItems: z.int().gt(0),
                    mediaType: z.enum(["audio", "image", "video"]),
                    minItems: z.int().min(0),
                    targetSlotId: z.string().min(1),
                  }),
                ),
                kind: z.enum(["video"]),
                maxInputItems: z.int().min(0),
                params: z.array(
                  z.object({
                    field: z.string().min(1),
                    numberMultiplier: z.optional(z.number().gt(0)),
                    sendWhen: z.optional(
                      z.object({
                        equals: z.union([z.boolean(), z.string(), z.number()]),
                        settingId: z.string().min(1),
                      }),
                    ),
                    settingId: z.string().min(1),
                    valueMap: z.optional(
                      z
                        .object({})
                        .catchall(
                          z.union([z.boolean(), z.string(), z.number()]),
                        ),
                    ),
                  }),
                ),
                promptField: z.nullable(z.string().min(1)),
                settingIds: z.array(z.string().min(1)),
                staticParams: z
                  .object({})
                  .catchall(z.union([z.boolean(), z.string(), z.number()])),
              }),
            ]),
          }),
        ]),
      }),
      executionMode: z.enum(["debug", "live"]),
      inputAssets: z.array(
        z.object({
          assetId: z.string(),
          durationSeconds: z.nullable(z.number()),
          height: z.nullable(z.int()),
          mimeType: z.string(),
          providerUrl: z.url(),
          sizeBytes: z.nullable(z.int().min(0)),
          width: z.nullable(z.int()),
        }),
      ),
      job: z.object({
        flowRunId: z.string(),
        id: z.string(),
        itemKey: z.string(),
        mediaType: z.enum(["image", "video", "audio", "text"]),
        nodeId: z.string(),
        providerJobId: z.nullable(z.string()),
        providerSubmittedAt: z.nullable(z.iso.datetime()),
        submissionState: z.enum(["not_started", "submitting", "submitted"]),
        status: z.enum(["running"]),
      }),
      request: z.object({
        adapterRequestVersion: z.literal(3),
        catalogRevision: z.string().regex(/^sha256:[0-9a-f]{64}$/),
        catalogVersion: z.int().gt(0),
        itemKey: z.string(),
        modelContractVersion: z.string(),
        nodeId: z.string(),
        operationId: z.string(),
        orderedInputs: z.array(
          z.object({
            edgeId: z.string(),
            items: z.array(
              z.object({
                assets: z.array(
                  z.object({
                    assetId: z.string(),
                    mediaType: z.enum(["audio", "document", "image", "video"]),
                    order: z.int().min(0),
                  }),
                ),
                dimensions: z.object({}).catchall(z.string()),
                itemKey: z.string(),
                text: z.nullable(z.string()),
              }),
            ),
            order: z.int().min(0),
            sourceHandleId: z.string(),
            sourceNodeId: z.string(),
            targetSlotId: z.string(),
          }),
        ),
        outputCount: z.int().gt(0),
        productModelId: z.string(),
        modelRevision: z.int().gt(0),
        requestId: z.string(),
        requestIndex: z.int().min(0),
        requestPayloadHash: z.string(),
        settings: z
          .object({})
          .catchall(z.union([z.boolean(), z.string(), z.number()])),
        textSlots: z.array(
          z.object({
            inputReferences: z.array(
              z.object({
                assetId: z.string(),
                index: z.int().min(0),
                itemKey: z.string(),
                mediaType: z.enum(["audio", "image", "video"]),
                partIndex: z.int().min(0),
                slotId: z.string(),
                sourceNodeId: z.string(),
              }),
            ),
            parts: z.array(
              z.object({
                edgeId: z.nullable(z.string()),
                itemKey: z.nullable(z.string()),
                order: z.int().min(0),
                source: z.enum(["connected", "inline"]),
                sourceNodeId: z.nullable(z.string()),
                text: z.string(),
              }),
            ),
            resolvedText: z.string(),
            slotId: z.string(),
            source: z.enum(["connected", "inline"]),
          }),
        ),
      }),
    }),
  ),
}) as unknown as z.ZodType<PostRunsIdBrowserJobsClaim200>;

/**
 * @description Validation error
 */
export const postRunsIdBrowserJobsClaim400Schema = z.lazy(
  () => errorResponseSchema,
) as unknown as z.ZodType<PostRunsIdBrowserJobsClaim400>;

/**
 * @description Authentication required
 */
export const postRunsIdBrowserJobsClaim401Schema = z.lazy(
  () => errorResponseSchema,
) as unknown as z.ZodType<PostRunsIdBrowserJobsClaim401>;

/**
 * @description Active organization required
 */
export const postRunsIdBrowserJobsClaim403Schema = z.lazy(
  () => errorResponseSchema,
) as unknown as z.ZodType<PostRunsIdBrowserJobsClaim403>;

/**
 * @description Resource not found
 */
export const postRunsIdBrowserJobsClaim404Schema = z.lazy(
  () => errorResponseSchema,
) as unknown as z.ZodType<PostRunsIdBrowserJobsClaim404>;

/**
 * @description Resource state conflict
 */
export const postRunsIdBrowserJobsClaim409Schema = z.lazy(
  () => errorResponseSchema,
) as unknown as z.ZodType<PostRunsIdBrowserJobsClaim409>;

/**
 * @description Organization request rate limit exceeded
 */
export const postRunsIdBrowserJobsClaim429Schema = z.lazy(
  () => errorResponseSchema,
) as unknown as z.ZodType<PostRunsIdBrowserJobsClaim429>;

/**
 * @description Internal server error
 */
export const postRunsIdBrowserJobsClaim500Schema = z.lazy(
  () => errorResponseSchema,
) as unknown as z.ZodType<PostRunsIdBrowserJobsClaim500>;

export const postRunsIdBrowserJobsClaimMutationRequestSchema = z.object({
  executorId: z.string().min(16).max(200),
  fenceToken: z.int().gt(0),
  get activeJobIds() {
    return z.array(cuid2Schema).max(4).optional();
  },
  limit: z.optional(z.int().min(1).max(4).default(2)),
}) as unknown as z.ZodType<PostRunsIdBrowserJobsClaimMutationRequest>;

export const postRunsIdBrowserJobsClaimMutationResponseSchema = z.lazy(
  () => postRunsIdBrowserJobsClaim200Schema,
) as unknown as z.ZodType<PostRunsIdBrowserJobsClaimMutationResponse>;
