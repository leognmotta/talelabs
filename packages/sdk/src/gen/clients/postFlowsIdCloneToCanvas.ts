/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../client";
import type { Client, RequestConfig, ResponseErrorConfig } from "../../client";
import type {
  PostFlowsIdCloneToCanvasMutationRequest,
  PostFlowsIdCloneToCanvasMutationResponse,
  PostFlowsIdCloneToCanvasPathParams,
  PostFlowsIdCloneToCanvas400,
  PostFlowsIdCloneToCanvas401,
  PostFlowsIdCloneToCanvas403,
  PostFlowsIdCloneToCanvas404,
  PostFlowsIdCloneToCanvas409,
  PostFlowsIdCloneToCanvas429,
  PostFlowsIdCloneToCanvas500,
} from "../types/PostFlowsIdCloneToCanvas.ts";
import {
  postFlowsIdCloneToCanvasMutationResponseSchema,
  postFlowsIdCloneToCanvasMutationRequestSchema,
} from "../zod/postFlowsIdCloneToCanvasSchema.ts";

function getPostFlowsIdCloneToCanvasUrl({
  id,
}: {
  id: PostFlowsIdCloneToCanvasPathParams["id"];
}) {
  const res = { method: "POST", url: `/flows/${id}/clone-to-canvas` as const };
  return res;
}

/**
 * {@link /flows/:id/clone-to-canvas}
 */
export async function postFlowsIdCloneToCanvas(
  {
    id,
    data,
  }: {
    id: PostFlowsIdCloneToCanvasPathParams["id"];
    data: PostFlowsIdCloneToCanvasMutationRequest;
  },
  config: Partial<RequestConfig<PostFlowsIdCloneToCanvasMutationRequest>> & {
    client?: Client;
  } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const requestData = postFlowsIdCloneToCanvasMutationRequestSchema.parse(data);

  const res = await request<
    PostFlowsIdCloneToCanvasMutationResponse,
    ResponseErrorConfig<
      | PostFlowsIdCloneToCanvas400
      | PostFlowsIdCloneToCanvas401
      | PostFlowsIdCloneToCanvas403
      | PostFlowsIdCloneToCanvas404
      | PostFlowsIdCloneToCanvas409
      | PostFlowsIdCloneToCanvas429
      | PostFlowsIdCloneToCanvas500
    >,
    PostFlowsIdCloneToCanvasMutationRequest
  >({
    method: "POST",
    url: getPostFlowsIdCloneToCanvasUrl({ id }).url.toString(),
    data: requestData,
    ...requestConfig,
  });
  return postFlowsIdCloneToCanvasMutationResponseSchema.parse(res.data);
}
