/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../client";
import type { Client, RequestConfig, ResponseErrorConfig } from "../../client";
import type {
  GetProductQueryResponse,
  GetProductPathParams,
  GetProduct401,
  GetProduct403,
  GetProduct404,
} from "../types/GetProduct.ts";
import { getProductQueryResponseSchema } from "../zod/getProductSchema.ts";

function getGetProductUrl({
  productId,
}: {
  productId: GetProductPathParams["productId"];
}) {
  const res = { method: "GET", url: `/products/${productId}` as const };
  return res;
}

/**
 * {@link /products/:productId}
 */
export async function getProduct(
  { productId }: { productId: GetProductPathParams["productId"] },
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetProductQueryResponse,
    ResponseErrorConfig<GetProduct401 | GetProduct403 | GetProduct404>,
    unknown
  >({
    method: "GET",
    url: getGetProductUrl({ productId }).url.toString(),
    ...requestConfig,
  });
  return getProductQueryResponseSchema.parse(res.data);
}
