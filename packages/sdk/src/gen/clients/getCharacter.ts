/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../client";
import type { Client, RequestConfig, ResponseErrorConfig } from "../../client";
import type {
  GetCharacterQueryResponse,
  GetCharacterPathParams,
  GetCharacter401,
  GetCharacter403,
  GetCharacter404,
} from "../types/GetCharacter.ts";
import { getCharacterQueryResponseSchema } from "../zod/getCharacterSchema.ts";

function getGetCharacterUrl({
  characterId,
}: {
  characterId: GetCharacterPathParams["characterId"];
}) {
  const res = { method: "GET", url: `/characters/${characterId}` as const };
  return res;
}

/**
 * {@link /characters/:characterId}
 */
export async function getCharacter(
  { characterId }: { characterId: GetCharacterPathParams["characterId"] },
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetCharacterQueryResponse,
    ResponseErrorConfig<GetCharacter401 | GetCharacter403 | GetCharacter404>,
    unknown
  >({
    method: "GET",
    url: getGetCharacterUrl({ characterId }).url.toString(),
    ...requestConfig,
  });
  return getCharacterQueryResponseSchema.parse(res.data);
}
