/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../client";
import type { Client, RequestConfig, ResponseErrorConfig } from "../../client";
import type {
  ListBrandCharactersQueryResponse,
  ListBrandCharactersPathParams,
  ListBrandCharacters401,
  ListBrandCharacters403,
  ListBrandCharacters404,
} from "../types/ListBrandCharacters.ts";
import { listBrandCharactersQueryResponseSchema } from "../zod/listBrandCharactersSchema.ts";

function getListBrandCharactersUrl({
  brandId,
}: {
  brandId: ListBrandCharactersPathParams["brandId"];
}) {
  const res = { method: "GET", url: `/brands/${brandId}/characters` as const };
  return res;
}

/**
 * {@link /brands/:brandId/characters}
 */
export async function listBrandCharacters(
  { brandId }: { brandId: ListBrandCharactersPathParams["brandId"] },
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    ListBrandCharactersQueryResponse,
    ResponseErrorConfig<
      ListBrandCharacters401 | ListBrandCharacters403 | ListBrandCharacters404
    >,
    unknown
  >({
    method: "GET",
    url: getListBrandCharactersUrl({ brandId }).url.toString(),
    ...requestConfig,
  });
  return listBrandCharactersQueryResponseSchema.parse(res.data);
}
