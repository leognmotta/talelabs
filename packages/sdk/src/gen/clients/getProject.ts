/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../client";
import type { Client, RequestConfig, ResponseErrorConfig } from "../../client";
import type {
  GetProjectQueryResponse,
  GetProjectPathParams,
  GetProject401,
  GetProject403,
  GetProject404,
} from "../types/GetProject.ts";
import { getProjectQueryResponseSchema } from "../zod/getProjectSchema.ts";

function getGetProjectUrl({
  projectId,
}: {
  projectId: GetProjectPathParams["projectId"];
}) {
  const res = { method: "GET", url: `/projects/${projectId}` as const };
  return res;
}

/**
 * {@link /projects/:projectId}
 */
export async function getProject(
  { projectId }: { projectId: GetProjectPathParams["projectId"] },
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetProjectQueryResponse,
    ResponseErrorConfig<GetProject401 | GetProject403 | GetProject404>,
    unknown
  >({
    method: "GET",
    url: getGetProjectUrl({ projectId }).url.toString(),
    ...requestConfig,
  });
  return getProjectQueryResponseSchema.parse(res.data);
}
