/** Loading placeholders that preserve the selected Asset library layout. */

import { MediaLibrarySkeleton } from '../../../shared/components/media-library-card'

/** Mirrors grid or list geometry while the first library page is unresolved. */
export function AssetLibrarySkeleton() {
  return <MediaLibrarySkeleton />
}
