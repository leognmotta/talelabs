/** Canonical dashboard icon aliases for first-class TaleLabs product entities. */

import { IconComponents, IconPhoto } from '@tabler/icons-react'

/** Represents one canonical media Asset. */
export const AssetIcon = IconPhoto

/** Represents an Element as a reusable collection of reference images. */
export const ElementIcon = IconComponents
